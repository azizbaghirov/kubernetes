apiVersion: v1
kind: Pod
metadata:
   name: nginx
   labels:
     app: myapp
     tier: frontend
spec:
   containers:
   - name: nginx
     image: nginx
#  nodeSelector:
#    size: Large
#  nodeName: node02 - alternatively we can specify certain node for the pod to be scheduled
